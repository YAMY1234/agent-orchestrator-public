pyyaml>=6.0

# Dashboard (orch dashboard)
fastapi>=0.110
# Starlette 1.x currently deprecates the httpx-backed TestClient used by
# FastAPI; stay on the tested 0.x line until that transition is complete.
starlette>=0.46,<1
uvicorn>=0.27
# ttyd reverse proxy so the iframe stays same-origin with the dashboard
httpx>=0.27
websockets>=12.0
